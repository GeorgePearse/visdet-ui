[build]
  base = "mlflow/server/js"
  command = "yarn build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# SPA redirect - route all requests to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
